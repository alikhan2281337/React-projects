import React from 'react';
import Title from "../title/Title";

const Header = () => {
    return (
        <div>
            <Title name={'header'}/>
        </div>
    );
};

export default Header;